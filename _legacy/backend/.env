# Server Configuration
NODE_ENV=development
PORT=5000
GPS_PORT=5023

# Database Configuration (SQLite)
DATABASE_DIALECT=sqlite
DATABASE_STORAGE=./gps_tracking.sqlite
# Legacy URL kept for reference but ignored if DIALECT is sqlite
DATABASE_URL=postgresql://user:pass@localhost:5432/dbname

# Security
JWT_SECRET=dev-secret-key-123
JWT_EXPIRES_IN=7d

# Redis Configuration (Fallback to memory if not found)
REDIS_HOST=localhost
REDIS_PORT=6379

# CORS Configuration
CORS_ORIGIN=http://localhost:3001
